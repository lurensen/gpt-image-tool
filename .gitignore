# 工作区记忆目录（含个人笔记与项目记录，不入库）
.workbuddy/

# 本地服务器运行日志
server-crash.log

# 依赖与系统文件
node_modules/
.DS_Store
Thumbs.db
*.local

# 本机启动脚本：仅本地使用，不入库（用 git rm --cached 移除跟踪后本地文件仍在）
*.bat

# Cloudflare 构建产物（由 Deploy command 生成，不入库）
dist/
.wrangler/
tmp/
